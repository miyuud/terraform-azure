# Deprecated Stack

This `openai` Terragrunt stack is deprecated and intentionally skipped.

Use:
- `cc/dev/eastus2/national/ai-foundry`

Migration command sequence (run from the new stack directory):
1. `./import.sh`
2. `terragrunt plan`
